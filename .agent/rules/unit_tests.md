# Strict Unit Testing Requirement

Every new Python module (backend) must have a corresponding unit test file generated in the `tests/` directory.
Key rules include:
- Use `pytest` for all unit testing.
- Ensure minimum coverage of 80% for critical logic (e.g., detection pipeline, feature extraction).
- Test edge cases and normal traffic scenarios.
